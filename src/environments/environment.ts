export const environment = {
  // Indica que este es el entorno de desarrollo
  production: false,

  // URL del backend en local (Spring Boot, Node, etc.)
  // OJO: no es el puerto de Angular (4200), es el del backend
  apiUrl: 'http://localhost:8080/api'
};